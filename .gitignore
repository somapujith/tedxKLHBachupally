node_modules
dist
.preview
.DS_Store
.playwright-mcp
.env
.env.local
.env.*.local

# Secret key files — never commit
*.key
rzp-key.csv
*-key.csv

# Personal media / form exports — keep out of repo
Submit your image (File responses)/
Submit your image (File responses) 2/
WhatsApp Image *.jpeg

# stray root key-art source copies (real asset lives in src/assets)
/theme*.png
/aboutimage.png
/stage image.png
/shareef.png

# local IDE workspace file
Tedx.code-workspace
